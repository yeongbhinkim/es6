//Array.prototype.sort();
//원본 배열 요소를 정렬해서 반환하다. (유니코드값으로 정렬됨)

const arr = ['banana','apple', 'melon'];

const result = arr.sort(); //오름차순(유니코드 기준)
console.log(result);
console.log(arr);
console.log(arr.reverse()); //배열 역순하여 반환
